apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "shailesh-bank-chart.fullname" . }}-test-connection"
  labels:
    {{- include "shailesh-bank-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "shailesh-bank-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
