apiVersion: v1
kind: Service
metadata:
  name: {{ include "shailesh-bank-chart.fullname" . }}
  labels:
    {{- include "shailesh-bank-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "shailesh-bank-chart.selectorLabels" . | nindent 4 }}
